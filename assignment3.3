# acad-assignment3

public class example {
	
	int x; int y;
	 String name;
	 public static void main(String args[]) {
	 example pnt = new example();
	 System.out.println ("pnt is " + pnt.name + " " + pnt.x + " " + pnt.y);
	}

}


Output
pnt is null 0 0

Reason:
pnt.name = null because string value name doesnt assign to any value so default value of string is null.
pnt.x = 0 because integer value x doesnt assign to any value so default value of integer is 0.
pnt.y = 0 because integer value y doesnt assign to any value so default value of integer is 0.
